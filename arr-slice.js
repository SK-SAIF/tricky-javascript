const array1=[12,23,34,45,68,98];
const part=array1.slice(0,3); //slice(starting position's index, index of position upto before which will be sliced )...this is important
console.log(part);
console.log(array1);//after slice the main array doesn't change at all.